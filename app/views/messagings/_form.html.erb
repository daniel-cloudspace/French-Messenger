<%= form_for(@messaging) do |f| %>
  <% if @messaging.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@messaging.errors.count, "error") %> prohibited this messaging from being saved:</h2>

      <ul>
      <% @messaging.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hash %><br />
    <%= f.text_area :hash %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
